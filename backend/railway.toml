# Railway — serviço connect-api
# No painel: Root Directory = api

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

# Healthcheck
[deploy.healthcheck]
path = "/health"
interval = "30s"
timeout = "10s"
